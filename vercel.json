{
    "builds": [{
        "src": "contact/wsgi.py",
        "use": "@ardnt/vercel-python3.11-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "contact/wsgi.py"
        }
    ]
}